# نظام الفواتير المحسّن - ملخص الميزات

## الميزات الرئيسية المضافة:

### 1. **نسب الخصم المرن (Discount Percentage)**
   - إضافة حقل لإدخال نسبة الخصم (%)
   - الحساب: `discount = subtotal × (discountRate/100)`
   - يتم تطبيق الخصم على الإجمالي الفرعي أولاً

### 2. **رسوم التحصيل والتوصيل (Fees Percentage)**
   - إضافة حقل لإدخال نسبة الرسوم (%)
   - الحساب: `fees = afterDiscount × (feesRate/100)`
   - يتم تطبيق الرسوم بعد الخصم

### 3. **معاينة الفاتورة (Preview)**
   - زر "معاينة" يعرض الفاتورة بدون حفظ
   - يسمح بالتحقق من البيانات قبل الحفظ النهائي
   - يمكن طباعتها أو حفظها كـ PDF أو إرسالها عبر WhatsApp مباشرة من المعاينة

### 4. **رموز QR Code**
   - توليد رمز QR فريد لكل فاتورة
   - يحتوي على: رقم الفاتورة + اسم العميل + المبلغ الإجمالي + التاريخ
   - يظهر تلقائياً في الفاتورة عند المعاينة أو الحفظ

### 5. **إرسال عبر WhatsApp**
   - رسالة منسقة تتضمن:
     * رقم الفاتورة
     * بيانات العميل
     * تفاصيل البنود
     * الملخص المالي الكامل (خصم، رسوم، ضريبة)
     * المبلغ النهائي
   - يتم فتح WhatsApp تلقائياً برقم العميل
   - رسالة جاهزة للإرسال

### 6. **تصميم احترافي للفاتورة**
   - تخطيط إلكتروني عصري مع:
     * شعار المتجر ورقم الفاتورة
     * بيانات العميل ممثلة بشكل منظم
     * جدول بنود واضح مع الألوان
     * قسم مالي متقدم يعرض:
       - الإجمالي الفرعي
       - الخصم (بالأخضر إذا كان موجوداً)
       - الإجمالي بعد الخصم
       - الرسوم (بالأحمر إذا كانت موجودة)
       - الضريبة
       - المبلغ النهائي (مع تدرج ملون)
     * توقيع المسؤول
     * عبارة شكر للعميل

### 7. **حسابات مالية محسّنة**
   - تسلسل الحسابات:
     1. Subtotal (الإجمالي الفرعي من البنود)
     2. Discount = Subtotal × (Discount Rate / 100)
     3. After Discount = Subtotal - Discount
     4. Fees = After Discount × (Fees Rate / 100)
     5. Tax = After Discount × (Tax Rate / 100)
     6. Total = After Discount + Fees + Tax

### 8. **عمليات الفاتورة المتاحة**
   - عرض الفاتورة (View)
   - معاينة (Preview)
   - طباعة (Print)
   - تحميل كـ PDF (Download)
   - إرسال عبر WhatsApp (Send)
   - حذف (Delete)

## الملفات المعدلة:

### admin.html
- إضافة حقول الخصم والرسوم
- إضافة زر المعاينة

### script.js
- دالة `createInvoice()` - محسّنة لدعم الخصم والرسوم
- دالة `previewInvoice()` - جديدة لمعاينة بدون حفظ
- دالة `displayInvoiceModal()` - مصممة احترافياً مع QR Code
- دالة `sendInvoiceWhatsApp()` - جديدة لإرسال الفواتير
- دالة `displayInvoicesList()` - محسّنة مع زر WhatsApp
- دالة `resetInvoiceForm()` - محسّنة لتنظيف الحقول الجديدة

## متطلبات التشغيل:

1. مكتبة Font Awesome 6.4.0 (للأيقونات) ✓
2. مكتبة html2canvas (لتحويل HTML إلى صور) ✓
3. مكتبة jsPDF (لإنشاء ملفات PDF) ✓
4. مكتبة QRCode.js 1.0.0 (لتوليد رموز QR) ✓
5. localStorage (لحفظ البيانات) ✓

## ملاحظات مهمة:

- جميع الحسابات تتم على جانب العميل (Client-side)
- البيانات محفوظة في localStorage
- WhatsApp يتطلب رقم هاتف صالح وتطبيق WhatsApp مثبت
- صيغة رقم الهاتف: يتم تحويل الصفر الأول إلى 2 (للأرقام المصرية)
- الفواتير تحتفظ بجميع البيانات بما فيها نسب الخصم والرسوم

## أمثلة الاستخدام:

### إنشاء فاتورة:
1. ملء بيانات الفاتورة (الرقم، التاريخ، بيانات العميل)
2. إضافة البنود (المنتجات) بأسعارها
3. تحديد نسبة الخصم (اختياري)
4. تحديد نسبة الرسوم (اختياري)
5. الضغط على "معاينة" للتحقق
6. الضغط على "حفظ" لحفظ الفاتورة

### معاينة قبل الحفظ:
- تظهر الفاتورة بشكل احترافي
- يمكن طباعتها أو تحميلها كـ PDF
- يمكن إرسالها عبر WhatsApp مباشرة من المعاينة
- لا يتم حفظها في قاعدة البيانات عند المعاينة

### إرسال عبر WhatsApp:
- الضغط على أيقونة WhatsApp الخضراء
- تفتح نافذة WhatsApp برسالة منسقة
- يمكن تعديل الرسالة قبل الإرسال
- الرسالة تحتوي على جميع بيانات الفاتورة

---

تم التطوير: 2024
النسخة: 2.0 (مع نظام الفواتير المحسّن)
