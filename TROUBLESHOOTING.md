# دليل استكشاف الأخطاء وحلها 🔧

## ❌ المشاكل الشائعة وحلولها

### 1️⃣ المشكلة: "فشل في الاتصال بـ Google Sheets"

**الأسباب المحتملة:**
- API Key غير صحيح أو منتهي الصلاحية
- Sheet ID غير صحيح
- Google Sheets API غير مفعّل
- الشيت غير مُشارك مع "Anyone with the link"
- تم تجاوز الحد المسموح لاستخدام API

**الحلول:**

#### الحل الأول: التحقق من صحة Console
1. افتح الصفحة الرئيسية
2. اضغط F12 لفتح Console
3. ابحث عن رسائل الخطأ مثل:
   - "Error fetching from Google Sheets"
   - "API key not valid"
   - "Requested entity not found"

#### الحل الثاني: إعادة إنشاء API Key
1. اذهب إلى [console.cloud.google.com](https://console.cloud.google.com)
2. اذهب إلى Credentials
3. احذف الـ API Key القديم
4. أنشئ API Key جديد
5. حدد القيود على النطاق:
   - تطبيق Google Sheets API فقط
   - لا تقيد من أين (إذا كنت تستخدم الموقع محلياً)
6. ضع الـ API Key الجديد في `script.js`

#### الحل الثالث: التحقق من مشاركة الشيت
1. افتح Google Sheet
2. اضغط "Share"
3. تأكد من أن "General access" هو "Anyone with the link"
4. إذا لم يكن، غيّره إلى هذا الإعداد

#### الحل الرابع: استخدام البيانات المحلية
الموقع الآن يستخدم البيانات المحلية تلقائياً كحل احتياطي إذا فشل Google Sheets.

---

### 2️⃣ المشكلة: "لا توجد أصناف في الصفحة الرئيسية"

**الأسباب المحتملة:**
- صفحة Categories في Google Sheet فارغة
- لم يتم تحميل البيانات من Google Sheets
- صفحة db.json لا تحتوي على أصناف
- localStorage فارغ

**الحلول:**

#### الحل الأول: إضافة أصناف في Google Sheet
1. افتح Google Sheet
2. اذهب إلى صفحة `Categories`
3. أضف الأصناف بهذا التنسيق:
   ```
   A2: إلكترونيات    B2: fas fa-mobile-alt      C2: الأجهزة الإلكترونية والهواتف
   A3: ملابس        B3: fas fa-tshirt         C3: الملابس والأحذية
   A4: إكسسوارات    B4: fas fa-gem            C4: الإكسسوارات والمجوهرات
   ```

#### الحل الثاني: استخدام الأصناف الافتراضية
الموقع الآن يستخدم أصناف افتراضية تلقائياً إذا لم توجد بيانات:
- إلكترونيات
- ملابس
- إكسسوارات
- كتب
- رياضة
- منزل

#### الحل الثالث: التحقق من Console
افتح Console وابحث عن:
- "الأصناف من localStorage: X"
- "تم عرض الأصناف بنجاح"
- "لم يتم العثور على عنصر product-cats"

---

### 3️⃣ المشكلة: "صفحة الإدارة لا تفتح"

**الأسباب المحتملة:**
- ملف login.html لم يتم تحديثه
- نظام تسجيل الدخول لا يعمل
- script.js غير مربط في admin.html

**الحلول:**

#### الحل الأول: التحقق من صفحة تسجيل الدخول
1. افتح `login.html`
2. أدخل أي اسم مستخدم وكلمة مرور
3. سيتم توجيهك تلقائياً إلى `admin.html`

#### الحل الثاني: التحقق من Console
افتح Console في `admin.html` وابحث عن:
- "دالة fetchFromGoogleSheets موجودة"
- "جاري تحميل البيانات الأولية للوحة التحكم"
- "تم تحميل X منتج"

#### الحل الثالث: مسح بيانات تسجيل الدخول
1. افتح Console في `admin.html`
2. اكتب: `sessionStorage.clear()`
3. اضغط Enter
4. أعد تحميل الصفحة وسجل الدخول من جديد

---

### 4️⃣ المشكلة: "المنتجات لا تظهر"

**الأسباب المحتملة:**
- بيانات المنتجات فارغة في Google Sheet
- لم يتم تحميل البيانات من db.json
- localStorage فارغ
- دالة renderProducts لا تعمل

**الحلول:**

#### الحل الأول: التحقق من وجود بيانات
افتح Console وابحث عن:
- "عدد المنتجات: X"
- "تم عرض المنتجات"
- "إذا لم توجد بيانات بعد كل المحاولات"

#### الحل الثاني: إضافة منتجات في Google Sheet
1. افتح صفحة `Products` في Google Sheet
2. أضف منتج واحد على الأقل:
   ```
   A2: 1    B2: ساعة ذكية    C2: 1250    D2: 1800    E2: رابط الصورة    F2: رابط الصورة    G2: إلكترونيات    H2: 5    I2: مميز ✨    J2: وصف الساعة    K2: مواصفات    L2: 2024-12-31    M2: 4.8
   ```

#### الحل الثالث: استخدام منتجات من db.json
الموقع يستخدم منتجات من `db.json` كحل احتياطي.

---

### 5️⃣ المشكلة: "زر التحديث يظهر فشل"

**الأسباب المحتملة:**
- مشكلة في الاتصال بـ Google Sheets
- دالة syncWithGoogleSheets غير موجودة
- المتغيرات غير محدثة بشكل صحيح

**الحلول:**

#### الحل الأول: التحقق من Console
1. افتح Console في `admin.html`
2. اضغط زر "تحديث من Google Sheets"
3. ابحث عن:
   - "جاري المزامنة مع Google Sheets"
   - "نتيجة المزامنة"
   - "Google Sheets Sync Error"

#### الحل الثاني: استخدام البيانات المحلية
الآن الزر يستخدم البيانات المحلية تلقائياً إذا فشل Google Sheets.

#### الحل الثالث: إعادة تحميل الصفحة
1. اضغط Ctrl+F5 لإعادة تحميل الصفحة
2. حاول التحديث مرة أخرى

---

## 🔧 خطوات التشخيص السريع

### إذا لم تظهر الأصناف:
1. افتح Console (F12)
2. ابحث عن: "الأصناف من localStorage"
3. إذا كانت "0"، فقدّ الأصناف من localStorage:
   ```javascript
   localStorage.removeItem('storeCategories');
   location.reload();
   ```

### إذا لم تظهر المنتجات:
1. افتح Console
2. ابحث عن: "عدد المنتجات: X"
3. إذا كانت "0"، فقدّ المنتجات من localStorage:
   ```javascript
   localStorage.removeItem('storeProducts');
   location.reload();
   ```

### إذا فشل الاتصال بـ Google Sheets:
1. تحقق من صحة Console للخطأ المحدد
2. اتبع الحلول أعلاه حسب نوع الخطأ
3. الموقع سيعمل بالبيانات المحلية تلقائياً

---

## 📞 طلب المساعدة

إذا استمرت المشاكل بعد تجربة الحلول:
1. انسخ رسالة الخطأ من Console
2. أرسلها مع وصف للمشكلة
3. سأقوم بتحليلها وحلها

---

## ✅ قائمة التحقق قبل الإبلاغ عن المشكلة

- [ ] فتحت Console (F12) وراجعت الرسائل
- [ ] جربت إعادة تحميل الصفحة (Ctrl+F5)
- [ ] مسحت بيانات localStorage (كما هو موضح أعلاه)
- [ ] تحققت من صحة Google Sheet أنها تحتوي على بيانات
-   - صفحة Categories تحتوي على أصناف
-   - صفحة Products تحتوي على منتجات
- [ ] تحققت من أن الشيت مُشارك مع "Anyone with the link"
- [ ] جربت استخدام الأصناف والمنتجات الافتراضية