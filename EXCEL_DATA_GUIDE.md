# 📘 دليل نظام البيانات عبر Excel - متجر مشالى

هذا النظام يسمح لك بإدارة **كل بيانات المتجر** من ملف Excel واحد (`store-data.xlsx`) مرفوع على GitHub، والموقع يقرأه ويُحدّث تلقائياً.

---

## 🔄 كيف تعمل (خطوة بخطوة)

1. **حمّل** ملف `store-data.xlsx` من GitHub (افتح الملف في المستودع ثم اضغط Download، أو من لوحة التحكم زر "تصدير ملف Excel كامل").
2. **افتحه** في برنامج Microsoft Excel.
3. **عدّل** البيانات كما تريد (أضف/احذف/عدّل منتجات، أصناف، فيديوهات، كوبونات، إعدادات).
4. **احفظ** الملف بنفس الاسم `store-data.xlsx`.
5. **ارفع** الملف على GitHub (زر Upload files ثم Replace، أو اسحبه فوق الملف القديم).
6. **الموقع يتحدّث تلقائياً** عند أي زائر — لا حاجة لأي خطوة أخرى!

---

## 📊 أوراق العمل في الملف

| الورقة | الوظيفة |
|--------|---------|
| **تعليمات** | شرح الاستخدام (لا تعدّل بيانات فيها) |
| **Products** | كل المنتجات والعروض |
| **Categories** | أقسام المتجر |
| **Videos** | فيديوهات الأكاديمية |
| **Coupons** | أكواد الخصم |
| **Settings** | إعدادات المتجر (واتساب، فودافون، انستاباي) |
| **Notifications** | الإشعارات (شريط الأخبار + إشعارات الشراء) |
| **Statuses** | حالات المنتجات |

---

## ✍️ قواعد كتابة البيانات

### Products (المنتجات)
| العمود | الشرح |
|--------|-------|
| id | رقم فريد للمنتج (اتركه كما هو أو ضع أي رقم) |
| الاسم | اسم المنتج |
| السعر | السعر الحالي (رقم فقط) |
| السعر قبل الخصم | السعر القديم (اختياري - يظهر الخصم تلقائياً) |
| الوصف | وصف المنتج |
| الصورة الرئيسية | رابط URL للصورة |
| صور إضافية (|) | روابط صور إضافية مفصولة بـ `|` |
| الصنف | اسم الصنف (يجب أن يطابق اسم في ورقة Categories) |
| المخزون | عدد القطع، أو اكتب `out` إذا نفدت الكمية |
| الحالة | مثل: عرض خاص، جديد |
| الشارات (|) | شارات مثل 🔥 الأكثر مبيعاً (مفصولة بـ `|`) |
| المواصفات (|) | مواصفات المنتج (مفصولة بـ `|`) |
| نهاية العرض | تاريخ بصيغة `2026-12-31` (اتركه فارغاً إذا لا يوجد عرض) |
| فيديوهات (|) | روابط فيديوهات المنتج (اختياري) |
| التقييم / عدد التقييمات | أرقام التقييم |

### Categories (الأصناف)
- **الاسم**: اسم الصنف (إلكترونيات، ملابس...)
- **الأيقونة**: كود أيقونة Font Awesome مثل `fas fa-mobile-alt`

### Videos (الفيديوهات)
- **العنوان**: اسم الفيديو
- **الرابط**: رابط YouTube
- **الصنف**: اسم قسم الفيديو
- **النوع**: `free` (مجاني) أو `paid` (مدفوع)
- **المدة**: مثل `1:12:41`

### Coupons (الكوبونات)
- **الكود**: نص الكوبون
- **القيمة**: مقدار الخصم
- **النوع**: `percent` (نسبة مئوية) أو `fixed` (مبلغ ثابت)
- **تاريخ الانتهاء**: بصيغة `2026-12-31`
- **الحد الأدنى**: أقل قيمة طلب لتفعيل الكوبون

### Settings (الإعدادات)
- **whatsapp**: رقم الواتساب بصيغة دولية بدون `+` (مثل `201551831308`)
- **vodafone**: رقم فودافون كاش
- **instapay**: اسم مستخدم انستاباي
- **qr**: رابط صورة QR كود انستاباي (اختياري)
- **ticker**: نص شريط الأخبار السفلي (اختياري - الأفضل استخدام ورقة Notifications)
- **proof**: رسالة إشعار الشراء (اختياري - الأفضل استخدام ورقة Notifications)

### Notifications (الإشعارات والأخبار)
| العمود | الشرح |
|--------|-------|
| الرسالة | نص الإشعار أو الخبر |
| النوع | `ticker` = يظهر في شريط الأخبار أعلى الموقع \| `proof` = إشعار الشراء المنبثق |
| المدة (ثانية) | **سرعة** عرض الرسالة بالثواني — مثال: 5 = تظهر 5 ثوانٍ |
| فعال | `نعم` (يعرض) أو `لا` (يخفي) |

> شريط الأخبار يعرض **كل** رسائل نوع `ticker` بالتناوب، وكل رسالة تظهر حسب مدتها (السرعة) التي تحددها في عمود "المدة". إشعارات الشراء تعرض رسائل نوع `proof` عشوائياً بنفس الطريقة.

---

## 🖥️ من لوحة التحكم (Admin)

- افتح تبويب **النسخ الاحتياطي**.
- اضغط **"تصدير ملف Excel كامل"** لتحميل ملف Excel بكل البيانات الحالية.
- عدّل عليه وارفعه على GitHub.

---

## ⚠️ ملاحظات مهمة

1. **لا تحذف الصف الأول** (العناوين) من أي ورقة.
2. **الصور يجب أن تكون روابط URL** كاملة تبدأ بـ `https://` (لا يمكن وضع صور داخل Excel).
3. **احفظ الملف بصيغة `.xlsx`** وليس CSV.
4. إذا لم يتوفر ملف Excel على GitHub، **الموقع يرجع تلقائياً** لملف `db.json` (حل احتياطي).
5. يحتاج الموقع إنترنت لتحميل مكتبة قراءة Excel (SheetJS) من CDN.

---

## 🛠️ الملفات المضافة/المعدّلة

| الملف | الوظيفة |
|-------|---------|
| `store-data.xlsx` | ملف البيانات الرئيسي (جديد) |
| `excel-loader.js` | قراءة/تصدير ملف Excel (جديد) |
| `index.html` | إضافة مكتبة SheetJS + الـ loader (معدّل) |
| `academy.html` | إضافة مكتبة SheetJS + الـ loader (معدّل) |
| `admin.html` | إضافة الـ loader + زر تصدير Excel (معدّل) |
| `script.js` | جعل Excel المصدر الأساسي للبيانات (معدّل) |
| `.gitignore` | استبعاد ملفات النسخ الاحتياطي من الرفع (جديد) |
